<%= form_for(tabemono) do |f| %>
  <% if tabemono.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tabemono.errors.count, "error") %> prohibited this tabemono from being saved:</h2>

      <ul>
      <% tabemono.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :rank %>
    <%= f.number_field :rank %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
